# Artifact boundary

Generated sessions, private-data derivatives, fitted models, and local reports belong under ignored
subdirectories or, preferably, approved storage outside the Git worktree. Model and session manifests may
be released only after their data lineage and confidentiality status have been reviewed.

The checked-in package references existing public artifacts in the parent repository. It does not duplicate
model binaries or confidential structures.

